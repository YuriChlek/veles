import styles from "./page.module.scss";
import React from "react";

const Home = (): React.JSX.Element => {
    return (
        <main>
            <h2>Home page</h2>
        </main>
    );
}

export default Home;
